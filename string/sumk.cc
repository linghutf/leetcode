/**
 * 和为k的子序列
 */
#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;


void sumK(std::vector<std::vector<int> >& v,std::vector<int> &a,int k)
{
    if()
    sort(a.begin(),a.end());

    for(auto it = a.begin();it!=a.end();++it){

    }
}

int main(int argc, char *argv[])
{

    return 0;
}
